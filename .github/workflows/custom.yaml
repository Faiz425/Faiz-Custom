name: Faiz-Custom Action CI

on: [push]

jobs:
    build:
        runs-on: ubuntu-latest

        steps:
            - name: checkout code
              uses: actions/checkout@v6

            - name: hello world custom action
              uses: ./faiz-custom@main
              with:
                who-to-greet: 'Faiz'

