name: Faiz Custom Action CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Hello World Custom Action
        uses: Faiz425/Faiz-Custom@main
        with:
          name : "Faiz"


